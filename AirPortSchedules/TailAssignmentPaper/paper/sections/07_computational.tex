%%==========================================================================
\section{Computational Study}
\label{sec:computational}
%%==========================================================================

\subsection{Implementation and environment}

All models are implemented in Python~3.10+ using the Pyomo optimisation
framework~\citep{pyomo2023}. The compact benchmark in Table~\ref{tab:repro5}
and the matched diagnostic in Table~\ref{tab:matched_stress} were solved with IBM ILOG
CPLEX~\citep{cplex2023} through Pyomo's \texttt{cplex} shell backend. For the
new Constraint~(13) verification and per-check-type hierarchy study
(\Cref{subsec:comp_c13,subsec:hierarchy_results}) results are cross-validated
with two independent exact MILP solvers: the open-source HiGHS
solver~\citep{highs2023} through Pyomo's \texttt{appsi\_highs} interface, and
the full, unrestricted IBM ILOG CPLEX~12.6 build distributed with AMPL
through Pyomo's \texttt{cplexamp} ASL interface. The CPLEX copy already on
this machine's PATH is a size-restricted Community/Preview edition (1\,000
variable/constraint limit) that raises \texttt{CPLEX Error 1016} on every
instance in this study once maintenance constraints are included; it is not
used for any reported result. HiGHS and the unrestricted CPLEX build agree
exactly (same status, same objective) on every instance reported in
\Cref{subsec:hierarchy_results}, giving solver-independent confidence in the
correctness claims.
The code, instance generator, and experiment runner are publicly available at
the companion repository.

\paragraph{Comparison protocol.}
The compact benchmark and the matched diagnostic use the same JSON instances
and assignment-cost data for both formulations where a comparison is claimed.
They do not by themselves certify cumulative-hour correctness;
\Cref{subsec:comp_c13,subsec:hierarchy_results} evaluates the real built
constraints and independently replays maintenance counters.

\subsection{Instance generation}

The current generator creates deterministic synthetic instances. For
parameters $(\delta,n_p,n_d)$ it generates approximately
$\delta n_p n_d$ flights, chooses airports and times from seeded pseudo-random
distributions, and uses a stable seed for each test index. Thus $\delta$ is a
flight-count scaling parameter in this repository, not the fleet-to-minimum-
fleet ratio used by \citet{khaled2018compact}. Unless a table states otherwise,
the quick snapshots contain one generated instance per parameter cell.

\subsection{Reference benchmark}

For a compact numerical anchor, Table~\ref{tab:repro5} reports one exact solve
on the small \texttt{ABCD\_capacity\_bottleneck\_test} instance. The former
random snapshots that reproduced the configurations of Tables~6, 10, and~11
of \citet{khaled2018compact} are omitted: their
infeasible cells were neither matched across formulations nor informative for
the correctness claim.

\begin{table}[t]
\centering\small
\caption{Measured MILP benchmark on \texttt{ABCD\_capacity\_bottleneck\_test}.
  This compact row gives the actual solver statistics for one exact solve.}
\label{tab:repro5}
\begin{tabular}{l r r r}
\toprule
$\text{Status}$ & CPU (s) & Wall (s) & Obj \\
\midrule
optimal & 0.09 & 0.20 & 8\,300 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Numeric verification of the Constraint~(13) endpoint-split flaw}
\label{subsec:comp_c13}

Lemmas~\ref{lem:c13_flaw} and~\ref{lem:c13_split_limit} are proved
algebraically in \Cref{subsec:c13_flaw,subsec:c13_corrected}. We additionally
verify both directly against the production implementation:
\texttt{experiments/c13\_loophole\_validation.py} builds the real
\texttt{LegacyEndpointSplitMILPScheduler} model -- with both the split
formulation and, using the restored \texttt{use\_paper\_c13} toggle, the
original Khaled et al. (2018) form -- on the four-day, single-aircraft
instance \texttt{data/instances/c13\_loophole\_test.json}
($T^A_{\max}=600$~min, $600$~min flown on each of days~2 and~4), and
evaluates every real \texttt{m.c13} constraint row at two adversarial check
patterns.

\begin{table}[t]
\centering\small
\caption{Numeric verification of Lemmas~\ref{lem:c13_flaw}
  and~\ref{lem:c13_split_limit} against the real \texttt{m.c13} constraints
  (both formulations) and the exact-state recursion
  (\texttt{results/tables/c13\_loophole\_validation.csv}). Both scenarios fly
  1\,200 real minutes in window $(1,4]$ against $T^A_{\max}=600$.}
\label{tab:c13_loophole}
\begin{tabular}{lccc}
\toprule
Check pattern & Split C13 & Paper C13 & Exact-state \\
\midrule
Both endpoints checked (days 1, 4) & \textbf{accepts (wrong)} & rejects (correct) & rejects \\
Only end-day checked (day 4)       & rejects (correct) & \textbf{accepts (wrong)} & rejects \\
\bottomrule
\end{tabular}
\end{table}

\Cref{tab:c13_loophole} confirms both lemmas computationally, and shows the
two formulations fail in \emph{complementary, non-overlapping} cases, exactly
as predicted in \Cref{subsec:c13_corrected}: when checks occur at both
window endpoints, all 42 real split rows are satisfied even though 1\,200
minutes are flown against the 600-minute limit (Lemma~\ref{lem:c13_split_limit}),
while the single paper-form row correctly rejects it. When only the end day
is checked, the paper form's single row is satisfied despite the same
1\,200-minute violation (Lemma~\ref{lem:c13_flaw}), while all 42 split rows
correctly reject it (one row is violated). In both scenarios the exact-state
recursion \eqref{eq:c13_state_lower}--\eqref{eq:c13_state_bound}, evaluated on
the same flight and check data, correctly flags the violation. This is a
computational proof, not merely an algebraic one: the values above come from
evaluating the actual Pyomo constraint objects built by
\texttt{src/model.py}, with its real $M_{\text{BIG}}=9\,999\,999$ constant,
rather than a hand re-derivation of the inequalities.

\subsection{Legacy hierarchy diagnostics and per-check-type effectiveness}
\label{subsec:hierarchy_results}

\Cref{tab:hierarchy} reports \texttt{experiments/hierarchy\_effectiveness.py}:
for each check type in isolation (A, B, C, D) we solve the legacy
endpoint-split MILP on \texttt{ABCD\_near\_threshold\_test}, and for the
full mixed hierarchy on \texttt{ABCD\_check\_hierarchy\_test}. Results are
cross-validated with two independent exact solvers: the open-source HiGHS
solver (\texttt{appsi\_highs}) and the full, unrestricted IBM ILOG CPLEX 12.6
build distributed with AMPL (invoked through Pyomo's \texttt{cplexamp} ASL
interface); both report identical objectives and statuses on every instance
in this study. The CPLEX copy already on this machine's PATH is a
size-restricted Community/Preview edition (1\,000 variable/constraint limit,
\texttt{CPLEX Error 1016}) that cannot solve any instance with maintenance
constraints active; it is not used for any reported result. Every solved
instance is independently re-verified by replaying its solution: hour-triggered
checks (A, B) are checked with the
exact-state recursion of \Cref{subsec:c13_state}, and calendar-triggered
checks (C, D) are checked with the analogous day-gap recursion, both
implemented from scratch in the validator, not reused from the solver's own
constraints.

This table evaluates the legacy hierarchy implementation only. The event-based
model is not included because this experiment does not solve and replay a
matched event-based counterpart on the same hierarchy instances. Adding an
event column without that matched evidence would create a misleading
comparison; cross-formulation evidence is reported separately in
Table~\ref{tab:event_feasible_solved}.

\begin{table}[t]
\centering\small
\caption{Legacy-model per-check-type effectiveness on
  \texttt{ABCD\_near\_threshold\_test}
  (single check active) and the full hierarchy on
  \texttt{ABCD\_check\_hierarchy\_test}
  (\texttt{results/tables/hierarchy\_effectiveness.csv}). ``Genuine
  violations'' excludes cases fully explained by an aircraft that starts the
  horizon already over threshold ($\Phi_j>T^{c}_{\max}$), which violates the
  model's own initial-condition precondition \eqref{eq:c13b} regardless of
  formulation.}
\label{tab:hierarchy}
\begin{tabular}{lrrrrrr}
\toprule
Check(s) & Status & Obj & Vars & Cons. & Independently valid? & Genuine viol. \\
\midrule
A only     & optimal & 20\,000 & 648  & 1\,318 & yes & 0 \\
B only     & optimal & 20\,000 & 648  & 1\,516 & yes & 0 \\
C only     & optimal & 20\,500 & 648  & 1\,679 & yes & 0 \\
D only     & optimal & 20\,500 & 648  & 1\,679 & yes & 0 \\
Full A/B/C/D & optimal & 14\,700 & 1\,092 & 2\,445 & \textbf{no} & 1 of 4 \\
\bottomrule
\end{tabular}
\end{table}

For each check type solved in isolation, the legacy model returns an optimal,
fully covered schedule that the independent replay certifies as correct: zero
threshold violations across all four check types on
\texttt{ABCD\_near\_threshold\_test}. This is the practical evidence that the
corrected hierarchy model enforces every check type (hour-triggered A/B and
calendar-triggered C/D) without relaxation on realistic, non-adversarial
instances.

On the full mixed-hierarchy instance, the replay flags four aircraft/check
violations, of which three (aircraft~0's A and B checks, aircraft~1's B
check) are fully explained by the instance's deliberately overdue initial
state ($\Phi^A_0=27\,001>T^A_{\max}=27\,000$, similarly for $B$) -- a data
precondition violation that would also invalidate the exact-state model's own
initial condition \eqref{eq:c13b}, independent of which C13 formulation is
used. The remaining case is genuine: aircraft~2 starts with
$\Phi^A_2=26\,800<T^A_{\max}$ (not overdue), yet the legacy solution flies
enough on day~1 to reach 27\,200 minutes, exceeding the 27\,000-minute
threshold, a violation the legacy split model's own constraints do not
detect. This is a second, independently arising instance of the same
endpoint-split flaw proved in \Cref{lem:c13_split_limit} and
\Cref{tab:c13_loophole} -- found on an existing test instance under the full
hierarchy, not constructed by hand -- and it demonstrates that the flaw is
not confined to contrived corner cases: it can occur whenever the
hierarchy's aggregated $\mathrm{mega}$ indicator lets a heavier check on one
aircraft satisfy the endpoint condition for another aircraft's window in the
same solve.

\subsection{Measured effectiveness of the event-based reformulation}
\label{subsec:event_measurements}

\Cref{sec:event_reformulation} proves the event-based reformulation exact
and immune to the endpoint-split failure mode by construction. This
subsection measures its practical effectiveness against the day-indexed
legacy model on which it improves, using
\texttt{experiments/compare\_formulations.py}. While diagnosing the first
solved comparisons below, the same protocol used to certify results
elsewhere in this section -- always cross-checking a status against an
independent computation -- exposed an implementation defect in
\texttt{src/event\_model.py}: the pre-horizon initial-hours constraint used
a big-$M$ equal to the source flight's own duration plus its initial hours,
which is not a valid upper bound on $u_{ijk}$ once a route has accumulated
several flights, and made the model reject cost-optimal schedules it should
accept. The fix (using $T_k+p_i$, the same bound family as
\Cref{eq:event_c12}) is included in the code accompanying this paper; all
results below use the corrected version.

\paragraph{Solved comparison on a well-posed instance.}
On a small hand-built instance with two aircraft, six flights, and a
threshold comfortably above the total flying time
(\texttt{data/instances/event\_vs\_legacy\_clean\_test.json}), both
formulations now return the same optimal objective (600, a single aircraft
covering all six flights with no maintenance event), using CPLEX 12.6 for
AMPL as the solver of record. This is exact agreement between two
independently coded formulations on a genuinely solved instance, not merely
a build-time comparison.

\paragraph{A modelling-convention gap on the existing ABCD suite.}
On the existing \texttt{ABCD\_*} boundary instances
(for example \texttt{ABCD\_capacity\_bottleneck\_test}), the event-based
model reports \texttt{infeasible} where the legacy model reports
\texttt{optimal}. This is
not a solver or implementation defect: these instances' own design notes
(e.g. \texttt{ABCD\_capacity\_bottleneck\_test.json}) state that an aircraft
is intended to fly \emph{past} its hour threshold and only be checked that
same night -- exactly the permissive convention encoded in the legacy
model's C13b relaxation. The exact-state formulations of this paper (both
the day-indexed recursion of \Cref{subsec:c13_state} and the event-based
\Cref{eq:event_c11} bound $u_{ijk}\le T_k x_{ij}$ unconditionally) instead
require the threshold to never be exceeded at any arrival, check or no
check. Under that stricter, arguably safer reading, these particular
instances are genuinely infeasible: no schedule exists in which an aircraft
never exceeds its hour limit for these initial states and flight durations.
This is a modelling-convention gap between the existing benchmark suite and
the corrected formulations of this paper, not a defect in either
implementation, and it is a direct practical consequence of fixing the
endpoint-split flaw: the corrected model can no longer quietly accept the
same after-the-fact resolution the legacy convention relied on.

\paragraph{Model-size scaling versus planning horizon.}
Table~\ref{tab:event_scaling} reports \texttt{--build-only} statistics for
both formulations on the same fleet (10 aircraft) across horizons
$h\in\{7,15,21,30\}$ from the density-1.0 \texttt{DataCplex} grid.

\begin{table}[t]
\centering\small
\caption{Model size versus planning horizon $h$ (10 aircraft, density 1.0;
  \texttt{results/tables/formulation\_scaling.csv}). ``Vars ratio''/``Cons.\
  ratio'' are event-based over legacy.}
\label{tab:event_scaling}
\begin{tabular}{rrrrrrr}
\toprule
$h$ & Legacy vars & Event vars & Vars ratio & Legacy cons. & Event cons. & Cons.\ ratio \\
\midrule
7  & 10\,800  & 7\,866   & 0.73 & 15\,819  & 24\,874     & 1.57 \\
15 & 33\,400  & 23\,582  & 0.71 & 42\,927  & 91\,598     & 2.13 \\
21 & 61\,500  & 39\,398  & 0.64 & 78\,394  & 167\,211    & 2.13 \\
30 & 175\,430 & 210\,161 & 1.20 & 432\,700 & 1\,209\,411 & 2.80 \\
\bottomrule
\end{tabular}
\end{table}

The variable-count advantage predicted by
\Cref{tab:event_variable_counts} holds at $h=7,15,21$ (27--36\% fewer
variables) but reverses at $h=30$, where the event model uses 20\% \emph{more}
variables. Constraint count is larger for the event-based model at every
horizon tested, by a factor of 1.6--2.8, and this factor broadly increases
with $h$. The
reason is exactly the caveat already noted in
\Cref{sec:event_reformulation}: constraint~\eqref{eq:event_c12} is built over
every feasible connection $(i,\ell)\in\Conn$, and $|\Conn|$ grows
super-linearly with the number of flights on these dense
($\delta=1.0$) synthetic instances, whereas the legacy model's maintenance
constraint count scales with $n_pn_d$. The event-based reformulation is
therefore not unconditionally smaller in practice: its variable-count
advantage is real but bounded, and its constraint count is measurably larger
on this benchmark family, a genuine trade-off rather than a strict
improvement.

An exploratory density-0.5 horizon stress run is retained in the companion
repository as \texttt{results/tables/formulation\_horizon\_scaling.csv}, but
is excluded from runtime claims because its infeasible and time-limited rows
mix different feasible domains. The corrected independent replay also showed
that the legacy endpoint-split optimum on the h=10 exploratory instance can
violate the A/B flight-hour state limits. The controlled runtime comparison
below therefore uses only feasible heavy instances for which both formulations
are optimal and objective-matched.

The matched stress comparison also uncovered and corrected an objective-model
defect in the legacy implementation. The day-indexed model represents a
multi-day check by consecutive occupancy variables and previously charged a
flat penalty per occupied day. When a check extended beyond the planning
horizon, this underpriced the event relative to the event formulation, which
charges once per check using its full duration. The legacy model now includes
an explicit binary maintenance-start variable and charges the same
duration-based event cost as the event formulation. After this correction, the
$h=21$ heavy case has objective $881\,263$ in both formulations; the corrected
matched result is recorded in
\texttt{results/tables/formulation\_h21\_objective\_aligned\_v2.csv}.

\paragraph{A guaranteed-feasible instance generator.}
Both formulations are exact, so any solved-objective comparison between them
is only informative on instances that are actually feasible; the random
\texttt{DataCplex} generator can produce infeasible cells at higher densities,
which is uninformative noise for this specific comparison rather than a
property of either formulation. \texttt{src/generate\_feasible\_instances.py} instead builds
each aircraft an explicit hub-and-spoke rotation (spoke $\to$ maintenance
airport $\to$ spoke) with maintenance duration and turn-time slack built
into every leg, and verifies every seeded rotation against an independent
greedy timeline simulator before writing the instance, so the generated
schedule is feasible by construction rather than by chance. Re-running the
same $h\in\{7,15,21,30\}$, 10-aircraft, density-1.0 comparison on four such
instances, both formulations solve to a matching optimum on every instance.
The generator caps flights per rotation, so the $h\in\{15,21,30\}$ instances
coincide in flight count (and hence in model size) and differ only by random
seed; they test feasibility at equal size rather than a growing horizon:

\begin{table}[t]
\centering\small
\caption{Solved comparison on guaranteed-feasible instances
  (\texttt{results/tables/formulation\_feasible\_scaling.csv}), full CPLEX
  for AMPL, 180\,s time limit. Both formulations reach the identical
  objective on every instance. The $h\in\{15,21,30\}$ rows share model size
  because the feasible generator caps flights per rotation; they differ only
  by random seed.}
\label{tab:event_feasible_solved}
\begin{tabular}{lrrrrrr}
\toprule
Instance & Legacy status & Legacy obj. & Event status & Event obj. & Legacy vars/cons. & Event vars/cons. \\
\midrule
h=7  & optimal & 24\,935 & optimal & 24\,935 & 3\,980/7\,928   & 6\,865/30\,360 \\
h=15 & optimal & 29\,914 & optimal & 29\,914 & 7\,020/15\,494  & 8\,820/38\,730 \\
h=21 & optimal & 29\,886 & optimal & 29\,886 & 7\,020/15\,494  & 8\,820/38\,730 \\
h=30 & optimal & 29\,875 & optimal & 29\,875 & 7\,020/15\,494  & 8\,820/38\,730 \\
\bottomrule
\end{tabular}
\end{table}

This is the cleanest evidence in this paper for the correctness claim of
\Cref{prop:event_correct}: on four independently constructed, genuinely
feasible instances, two independently coded exact formulations -- built from
different variable sets, different constraint families, and (after the fix
above) different big-$M$ derivations -- agree exactly on the optimal cost
every time.

\paragraph{Runtime and model-size scaling.}
To complement the objective-parity tables, we measured the corrected legacy
and event formulations on three independently generated feasible heavy
instances with $h\in\{7,15,21\}$, ten aircraft, and density $0.5$. Every run
reported here was optimal, and the objective values matched exactly. The
figures use wall-clock time, active Pyomo variable count, and active constraint
count from \texttt{results/tables/formulation\_heavy\_scaling\_optimized.csv}.
They therefore compare execution and model size only on a common, solved
feasible domain.

\begin{figure}[t]
\centering
\includegraphics[width=0.86\linewidth]{figures/formulation_runtime_vs_horizon.png}
\caption{Wall-clock solve time versus horizon on objective-matched feasible
  heavy instances. The event formulation is slower on this sample, although
  both formulations return the same optimum.}
\label{fig:event_runtime_scaling}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.86\linewidth]{figures/formulation_variables_vs_horizon.png}
\caption{Active variable count versus horizon on the same feasible heavy
  instances. The event formulation uses fewer variables in these three cases.}
\label{fig:event_variables_scaling}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.86\linewidth]{figures/formulation_constraints_vs_horizon.png}
\caption{Active constraint count versus horizon on the same feasible heavy
  instances. The event formulation uses more constraints because its flow
  equations are generated over feasible event connections.}
\label{fig:event_constraints_scaling}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.86\linewidth]{figures/formulation_relative_time_ratio.png}
\caption{Relative event-based time on the objective-matched heavy instances.
  A ratio below one means the event formulation is faster; a ratio above one
  means it is slower. Build time can decrease at longer horizons, but solved
  wall time remains above the legacy value in every tested case.}
\label{fig:event_relative_time}
\end{figure}

The event formulation is therefore not claimed to be faster on this sample:
its solve wall time is approximately 6.74, 1.68, and 1.43 times the legacy
wall time at $h=7,15,21$, respectively, in this benchmark run. A decrease does
appear in model-construction time at the two longer horizons: the event/legacy
build-time ratios are 1.13, 0.84, and 0.69. Thus the event formulation reduces
build time after $h=7$ in this sample, but does not reduce total solved
execution time. These are single-run measurements and should be interpreted
as indicative rather than universal performance constants.
Its advantage is structural correctness and a smaller variable set on these
instances, while the additional connection constraints create a real
computational trade-off. This distinction is important: the event formulation
improves the invalid maintenance-state logic, but it is not a universal
runtime replacement for the day-indexed model.

\paragraph{A second implementation defect, found and fixed the same way.}
The same generator, run with \texttt{--maintenance-families ABCD}, initially
solved under the legacy model but returned \texttt{infeasible} under the
event-based model. Applying the same fault-isolation procedure used for
\Cref{eq:event_c13} (relax all binary variables, solve the LP, and
deactivate one named constraint block at a time) to
constraints~\eqref{eq:event_c14a}--\eqref{eq:event_c14b} found a second,
distinct defect: the candidate set for a calendar-check deadline was built
from \emph{cost-feasible} assignment arcs only, without checking whether the
candidate flight is actually \emph{reachable} by that aircraft's route (a
flight can be affordable for an aircraft under the cost matrix's ferry
penalty while having no predecessor path to it at all in this sparse
hub-and-spoke instance family). The deadline constraint could then require a
check on a flight the aircraft could never legally reach, an unsatisfiable
requirement masquerading as a routing infeasibility. A second, narrower
defect surfaced during the fix: the placeholder row inserted when no
candidate exists at all evaluated to a plain Python \texttt{False} instead
of a symbolic Pyomo constraint, raising a solver-side error rather than a
clean infeasible status. Both are fixed in the code accompanying this
paper (an explicit reachability check via the same predecessor sets used by
constraint~\eqref{eq:event_c2}, and a placeholder anchored on a real
variable). Root-causing the resulting failure on the regenerated instance
traced to a third defect, this time in the instance generator itself: the
calendar-check deadline slack was computed from the coarse departure-day
index rather than the outbound flight's actual arrival time, so the
day-rounding in \texttt{src/generate\_feasible\_instances.py} could place an
aircraft's own designated check-triggering flight a few minutes past its own
deadline. With all three fixes applied, the regenerated ABCD instance
solves \texttt{optimal} under both formulations with the identical objective
(10\,327), completing the cross-validation started above. We report the
diagnostic process itself, not only the fix, because it is the same
generic technique (LP relaxation plus single-block deactivation) used
earlier for Constraint~(13), applied here to a different constraint family
with a different, unrelated root cause.

\subsection{Correctness comparison of the maintenance formulations}

The central advantage of the event-based formulation is correctness of the
maintenance state, not feasibility on historical instances designed around a
more permissive convention. Table~\ref{tab:matched_stress} therefore compares
the formulations on two adversarial Constraint~(13) scenarios and on three
independently generated feasible heavy instances. The first two rows evaluate
the actual production constraints and the exact-state replay on the same
1\,200-minute violation; the last three rows test whether the stricter event
state changes the optimal assignment when a feasible schedule exists.

\begin{table}[t]
\centering\scriptsize
\caption{Correctness and objective-parity comparison of the maintenance
  formulations. The first two rows are the adversarial C13 scenarios from
  Table~\ref{tab:c13_loophole}; ``accepts'' means the built model constraints
  admit the violating point, and ``rejects'' means they do not. The final
  rows are feasible heavy instances solved with unrestricted CPLEX~12.6 for
  AMPL and a 60-second limit.}
\label{tab:matched_stress}
\resizebox{\linewidth}{!}{%
\begin{tabular}{l l l l l}
\toprule
Test & Legacy paper C13 & Legacy split C13 & Event exact-state & Objective parity \\
\midrule
C13: both endpoints checked & rejects & accepts (wrong) & rejects & correct rejection \\
C13: only end checked & accepts (wrong) & rejects & rejects & correct rejection \\
Feasible heavy, $h=7$ & -- & optimal & optimal & 827\,858 = 827\,858 \\
Feasible heavy, $h=15$ & -- & optimal & optimal & 1\,132\,028 = 1\,132\,028 \\
Feasible heavy, $h=21$ & -- & optimal & optimal & 881\,263 = 881\,263 \\
\bottomrule
\end{tabular}
}%
\end{table}

The table shows the intended result directly. The original paper C13 and the
endpoint-split strengthening each fail on one complementary adversarial
pattern, whereas the event formulation rejects both violations. On feasible
heavy instances, this stronger state logic does not sacrifice the optimum:
the event and corrected legacy implementations return identical objective
values at all three horizons. Historical boundary cases that rely on
after-threshold checking are excluded from this correctness table because
they test a different maintenance convention rather than the quality of the
event reformulation.

\subsection{Summary of findings}

\begin{enumerate}
  \item The current runs validate the corrected formulations on deterministic
    synthetic instances; they do not reproduce the published data or
    averages.
  \item The Constraint~(13) endpoint-split flaw of \Cref{lem:c13_split_limit}
    is confirmed computationally: all 42 real constraint rows built by
    \texttt{src/model.py} for the loophole instance are satisfied at an
    adversarial point where 1\,200 minutes are actually flown against a
    600-minute limit, while the exact-state recursion correctly rejects it.
  \item Solved in isolation, each of the four check types (A, B, C, D) is
    enforced correctly by the legacy hierarchy model on
    \texttt{ABCD\_near\_threshold\_test}: optimal, fully covered, and
    independently validated with zero violations.
  \item Under the full mixed hierarchy, the same flaw reappears
    unprompted on an existing test instance (aircraft~2 in
    \texttt{ABCD\_check\_hierarchy\_test} exceeds its A-check threshold by
    200 minutes with no violated constraint reported), showing the issue is
    not confined to hand-built corner cases.
  \item The correctness comparison shows that the event formulation rejects
    both adversarial C13 violations, while the original paper row and the
    endpoint-split legacy formulation each fail on one complementary pattern.
  \item After correcting legacy multi-day maintenance-event pricing, the
    legacy and event formulations reach identical objectives on all three
    feasible heavy instances reported in Table~\ref{tab:matched_stress}.
  \item Historical boundary cases that depend on after-threshold checking are
    excluded from the correctness comparison because they use a different
    maintenance convention; dense exploratory cases remain solver diagnostics.
  \item The CPLEX installation available in this environment is a
    size-restricted Community/Preview edition and cannot solve any instance
    with maintenance constraints active (\texttt{CPLEX Error 1016}); the
    new Constraint~(13) and hierarchy results therefore use the open-source
    HiGHS solver, which is exact and unrestricted.
\end{enumerate}

\subsection{Phase 2: Structural trial of event-based maintenance bridge modes}
\label{subsec:phase2_bridge_modes}

\Cref{sec:event_reformulation} presents the full event-based reformulation as
the target model. The migration from the day-indexed legacy model to the
event-based formulation is implemented incrementally via flag-gated feature
toggles: new event-based variables and constraints coexist with legacy logic,
and bridge constraints control their interaction during the transition phase.
This subsection measures the structural impact (variable and constraint count)
of two event-based bridge modes on the existing test suite, without claiming
equivalence to solved objectives (which remain ``unknown'' under the
size-restricted solver in the current environment).

The three model configurations compared are:
\begin{itemize}
  \item \texttt{legacy}: the baseline day-indexed model (C1--C14 in day-indexed form);
  \item \texttt{event\_strict\_bridge}: event variables (set $E$ of \Cref{sec:event_reformulation}) with equality bridge constraints to day-indexed counterparts;
  \item \texttt{event\_only\_block\_capacity}: event maintenance only, with C8 (incompatibility blocking) and C9 (capacity) modeled as event constraints, legacy C8/C10 deactivated.
\end{itemize}

\begin{table}[ht]
\centering\small
\caption{Phase 2 per-instance structural deltas on ABCD test suite
  (\texttt{results/tables/phase2\_event\_trial\_compare\_full\_delta.csv}).
  Columns report the change in variable count and constraint count for each
  bridge mode relative to the legacy baseline.}
\label{tab:phase2_structural_deltas}
\begin{tabular}{lrrrr}
\toprule
Instance & $\Delta$Vars (strict) & $\Delta$Cons (strict) & $\Delta$Vars (event-only) & $\Delta$Cons (event-only) \\
\midrule
ABCD\_capacity\_bottleneck\_test.json & 144 & 572 & 144 & 249 \\
ABCD\_check\_hierarchy\_test.json & 351 & 1355 & 351 & 403 \\
ABCD\_near\_threshold\_test.json & 960 & 3640 & 960 & 1376 \\
ABCD\_no\_maint\_test.json & 392 & 1453 & 392 & 1109 \\
ABCD\_two\_b\_one\_c\_test.json & 480 & 1783 & 480 & 67 \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:phase2_structural_deltas} shows that both bridge modes add
event-based variables (uniformly $+144$ to $+960$ depending on instance size),
while the strict-bridge mode adds substantially more constraints (spanning
$+572$ to $+3640$) due to the equality constraints linking event and legacy
maintenance states. The event-only mode reduces constraint overhead to between
$+67$ and $+1376$, accepting the trade-off of legacy constraint deactivation.

Aggregate statistics over the five valid test cases (two instances excluded
due to solver memory/time limits under the preview CPLEX build) are:

\begin{table}[ht]
\centering\small
\caption{Phase 2 aggregate delta statistics
  (\texttt{results/tables/phase2\_event\_trial\_compare\_full\_agg.csv}).
  Mean and median are computed over the five non-error instances.}
\label{tab:phase2_aggregate_stats}
\begin{tabular}{lrrrrr}
\toprule
Metric & Count & Mean & Median & Min & Max \\
\midrule
$\Delta$ Vars (strict) & 5 & 465.4 & 392 & 144 & 960 \\
$\Delta$ Cons (strict) & 5 & 1760.6 & 1453 & 572 & 3640 \\
$\Delta$ Vars (event-only) & 5 & 465.4 & 392 & 144 & 960 \\
$\Delta$ Cons (event-only) & 5 & 640.8 & 403 & 67 & 1376 \\
CPU (legacy) & 5 & 0.025 & 0.025 & 0.023 & 0.028 \\
CPU (strict) & 5 & 0.029 & 0.030 & 0.024 & 0.033 \\
CPU (event-only) & 5 & 0.028 & 0.030 & 0.024 & 0.031 \\
\bottomrule
\end{tabular}
\end{table}

These measurements validate the implementation of both bridge modes on the
test suite: event variables are introduced without catastrophic growth in
model size, and model-building time increases modestly (mean $\approx 15\%$
for strict, $\approx 10\%$ for event-only). The event-only configuration
achieves a $2.7\times$ reduction in constraint overhead relative to strict
mode (640.8 vs.\ 1760.6 mean delta), confirming the structural benefit of
removing dual-state bridge constraints once the event model is ready for
production use. The correctness of both formulations and the soundness of
the migration strategy are not claimed on this subset of solver diagnostics
alone; they require the full correctness evidence and solved equivalence
tests from earlier subsections of this computational study.
